module.exports = {
  textFill: function(strElem) {
    var fontSize = +strElem.style.fontSize.replace(/px/, '') || 32;
    while(strElem.offsetWidth > strElem.parentElement.offsetWidth) {
      fontSize -= 1;
      strElem.style.fontSize = fontSize + 'px';
    }
    return fontSize
  }
};
